# Upload option assets

The camera SVG and Google Drive / OneDrive marks are exact exports from the upload-sheet reference in Figma file `QyA41WvdnWtnkJr2GONIMH`, option container `813:9151`, retrieved September 9, 2026. Camera is recolored with a CSS alpha mask using Ovie's icon token; brand marks retain their original colors. The link glyph is the existing local Lucide Link 2 asset, copied here so the sheet does not depend on unrelated uncommitted assets.

The implementation follows the root DESIGN.md rather than the reference file's older serif typography and teal palette. File selection and processing are local prototype behavior; no cloud authorization or link-delivery service is connected.
